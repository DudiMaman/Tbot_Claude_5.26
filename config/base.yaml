capital_usd: 5000.0
risk_per_trade_pct: 0.01       # 1% max risk per trade
max_daily_loss_pct: 0.03       # 3% daily drawdown circuit breaker
max_open_positions: 10
min_r_after_fees: 1.5          # minimum R:R after all fees/slippage
risk_mode: normal              # defensive | normal | aggressive
execution_mode: paper          # backtest | paper | live

# Consecutive-loss streak thresholds
# Low-WR trend strategies (10% WR): P(5 in a row) = 59%, P(12 in a row) = 28%.
# These thresholds must be high enough that normal losing streaks don't trigger halts.
losing_streak_defensive: 10   # switch to DEFENSIVE after this many consecutive losses
losing_streak_halt: 20        # halt new entries (P(20 in a row @ 10% WR) = 12%)
losing_streak_halt_reset_bars: 200  # auto-resume after N bars (200 × 15m = 50h cool-down)

# Crypto/stock exposure caps (portfolio level)
max_crypto_exposure_pct: 0.80
max_stock_exposure_pct: 0.40

# Stale order timeout
stale_order_timeout_minutes: 30
